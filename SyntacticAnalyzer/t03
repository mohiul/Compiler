/*
	Test Case 3: complex expressions (all arithmetic, relational and logic operators in one expression)
*/
class Utility{
	int i;
};
program {
	int a;
	float b;
	int c[1][2][3];
	Utility u;
	Utility uArr[2][3];
	
	a = 1;
	b = 1.0;
	c[0][1][2] = 5 * a + 7 and a * 4 + 2 > 1;
	c[0][0][0] = 5 * a + 7 and a * 4 + 2 > c[0][0][0] == 2 < a > uArr[1][2].i;
	a = uArr[1][2].i;
	u.i = randomize();
	
};
int randomize()
{
	int value;
	value = 1;
	return (value);
};